<%# 页面 %>

<%# 描述这个文件用于展示一些自定义的页面 %>
<%# 这个文件不接收参数 %>

<% if (page.title) { %>
    <%- partial("partial/topbar", {active: page.title}) %>
    <%- partial("page/" + page.title); %>
<% } else { %>
    <%- partial("page/default"); %>
<% } %>
